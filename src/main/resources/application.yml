spring:
  datasource:
    url: jdbc:mysql://localhost:3309/quizdb
    username: quizuser
    password: quizuser
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: Question-Service
#    properties:
#      hibernate:
#        format_sql: true
#        dialect: org.hibernate.dialect.MySQLDialect
eureka:
  instance:
    hostname: localhost

